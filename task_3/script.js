const num1 = 20;
var num2 = 30;
var canPass = true;
if (canPass)
    if (num1 > num2) {
        //  block of code to be executed if the condition is true
        console.log("Hello");
    }
    else { console.log("Bye"); }

else {
    console.log("Try again!");
}
